<% items.each do |item| %>
  <div class='menu item' data="<%= item.id %>">
    <div class: 'menu item-img-wrapper'>
      <%= cl_image_tag item.image_url.to_s, class: 'menu item-img' %>
    </div>
    <div class='menu item-title'>
      <p><%= item.title %></p>
    </div>
  </div>
<% end %>