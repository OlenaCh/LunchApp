<h2 class='header'>Add a menu item:</h2>
<%= render partial: 'items/form', locals: { item: item, 
                                            types: types,
                                            image: nil,
                                            action: 'Add' } %>